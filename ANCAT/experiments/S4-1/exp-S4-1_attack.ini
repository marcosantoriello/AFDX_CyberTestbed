#
# Attack configuration for S4-1 — ES flooding (S2-1) with switch-side policing
# for VL1 disabled (S3-3, scoped to VL1 only).
#
# ES0 acts as the malicious end system: its regulator forwards every frame
# immediately without BAG scheduling.
#
# Usage: place this file alongside exp-S4-1.xlsx in experiments/.
#        ANCAT_run.sh will automatically pass it to the simulator.
#

**.ESGroup[0].regulatorType = "MaliciousRegulator_Flooding"
