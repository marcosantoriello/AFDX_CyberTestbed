#
# Attack configuration for ES1 — VL Flooding (BAG bypass)
#
# ES[0] acts as the malicious end system: its regulator forwards every
# frame immediately without BAG scheduling, flooding the shared switch port.
#
# Usage: place this file alongside exp-ES1.xlsx in experiments/.
#        ANCAT_run.sh will automatically pass it to the simulator.
#

**.ESGroup[0].regulatorType = "MaliciousRegulator_Flooding"
